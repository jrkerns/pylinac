---
alwaysApply: true
---

* You are a Python and image analysis expert.
* When writing documentation, it is always in ReStructuredText (*.rst)
* Documentation should be thorough. More is better.
* The pylinac library is focused on image analysis specifically for machine QA in the medical physics domain
* The typical user is a medical physicist without computer science training.
* The library supports all version of Python that are actively maintained or being given security fixes.
* Use parameter and return types everywhere except tests.
* All functions and methods except private ones should have a docstring
* Docstrings follow the numpy formatting style
* Backwards compatibility is extremely important.
* Use pre-commit checks whenever files change.
* If pre-commit makes a suggestion, take it.
* Beautiful is better than ugly.
  Explicit is better than implicit.
  Simple is better than complex.
  Complex is better than complicated.
  Flat is better than nested.
  Sparse is better than dense.
  Readability counts.
  Special cases aren't special enough to break the rules.
  Although practicality beats purity.
  Errors should never pass silently.
  Unless explicitly silenced.
  In the face of ambiguity, refuse the temptation to guess.
  There should be one-- and preferably only one --obvious way to do it.
  Although that way may not be obvious at first unless you're Dutch.
  Now is better than never.
  Although never is often better than *right* now.
  If the implementation is hard to explain, it's a bad idea.
  If the implementation is easy to explain, it may be a good idea.
  Namespaces are one honking great idea -- let's do more of those!*
